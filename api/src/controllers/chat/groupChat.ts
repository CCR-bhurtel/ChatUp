import { Response } from 'express';
import { ExpressRequest } from '../../Types/User';
import catchAsync from '../../utils/catchAsync';

export const createGroupChat = catchAsync(async (req: ExpressRequest, res: Response, next) => {});

export const updateGroupChat = catchAsync(async (req: ExpressRequest, res: Response, next) => {});
